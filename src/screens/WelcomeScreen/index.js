import React, {useState} from "react"
import {Image, TouchableOpacity, View} from "react-native"
import {TitleCustom} from "../../components/UI/TitleCustom";

export const Welcome = ({navigation}) => {

    return (
        <View>
            <TitleCustom text={'Test'} fontSize={100}/>
        </View>
    )
}
