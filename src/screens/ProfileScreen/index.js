import React from 'react'
import {Text, View} from "react-native"

export const ProfileScreen = ({navigation}) => {
    return (
        <View>
            <Text>Profile</Text>
        </View>
    )
}
